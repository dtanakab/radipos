= render "programs/search_results/search_bar"
.contents
  =render "profile"
  br
  .tab_area
    .tabs
      .tab_icon.selected_icon
        =link_to image_tag("/assets/setting_icon_selected"), current_user
      .tab_icon
        =link_to image_tag("/assets/blank_star_icon"), user_likes_path(current_user)
      .tab_icon
        =link_to image_tag("/assets/sended_icon"), user_posted_mails_path(current_user)

  .setting_area
    h2
      | ラジオメールの設定
    = render "sample_mail", user:@user
    .profile_edit_link
      = link_to "ラジオメール設定/プロフィール情報を編集", edit_user_path
